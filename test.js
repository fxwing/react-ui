const arr = [1, 2, 3, 4, 5];

forEach  map  filter  some  every  find reduce  
  for for of  for  in  
  Object.entries  Reflect


  a  b  
[1-10] setState((state)=)

let s
for（let i= str.）{
s+=
}

Array.isArray
Object.prototype.toString
constructor = ''



function sleep(time){
 
    while(let now = Date.now();Date.now-now<time){}
    // return  new Promise((resolve)=>setTimeout(resolve,await))
}
